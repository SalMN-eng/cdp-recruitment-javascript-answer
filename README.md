# Requested setup
$ npm install

# Install mocha
$ npm install --save-dev mocha

# Launch (one of the parameters needed)
$ node app.js --filter=ry
#
$ node app.js --count

# Run Test units
$ npm run test
